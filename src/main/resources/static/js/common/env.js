import data from "../common/env.json" with { type: "json" };

export const firebaseConfig = data.firebaseConfig;

export const vapidKey = data.vapidKey;

export const KAKAO_CLIENT_ID = data.KAKAO_CLIENT_ID

export const KAKAO_REDIRECT_URI = data.KAKAO_REDIRECT_URI
